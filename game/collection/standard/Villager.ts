import { Role } from '@bezier/werewolf-core';
import CollectionEntry from '../CollectionEntry';

const villager: CollectionEntry = {
	role: Role.Villager,
	name: '村民',
	description: '你入夜后就睡下了，这一夜一如既往的安稳。天亮后，请仔细找出混入你们中间的狼人，投出狼人获得胜利。',
};

export default villager;
